<!-- Write 6 tiny unit tests (Jasmine)
	•	Bar (single series): labels/data align; legend off.
	•	Bar (multi series): datasets === yKeys; legend on.
	•	Line (area): returns 'line' config; dataset has fill: true.
	•	Doughnut (happy): labels/values map; colors applied.
	•	Missing x: friendly error with row number + key.
	•	Bad y: friendly error with row number + key.
